/**
 * @package @bitrix24/b24jssdk
 * @version 2.0.0
 * @copyright (c) 2026 Bitrix24
 * @license MIT
 * @see https://github.com/bitrix24/b24jssdk
 * @see https://bitrix24.github.io/b24jssdk/
 */
'use strict';

const ping = require('./ping.cjs');
const healthcheck = require('./healthcheck.cjs');
const loggerFactory = require('../../logger/logger-factory.cjs');

var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
const pingName = /* @__PURE__ */ Symbol("ping");
const healthCheckName = /* @__PURE__ */ Symbol("healthCheck");
class ToolsManager {
  static {
    __name(this, "ToolsManager");
  }
  _b24;
  _logger;
  _mapTools;
  constructor(b24) {
    this._b24 = b24;
    this._logger = loggerFactory.LoggerFactory.createNullLogger();
    this._mapTools = /* @__PURE__ */ new Map();
  }
  setLogger(logger) {
    this._logger = logger;
  }
  getLogger() {
    return this._logger;
  }
  get ping() {
    if (!this._mapTools.has(pingName)) {
      this._mapTools.set(pingName, new ping.Ping(this._b24, this._logger));
    }
    return this._mapTools.get(pingName);
  }
  get healthCheck() {
    if (!this._mapTools.has(healthCheckName)) {
      this._mapTools.set(healthCheckName, new healthcheck.HealthCheck(this._b24, this._logger));
    }
    return this._mapTools.get(healthCheckName);
  }
}

exports.ToolsManager = ToolsManager;
//# sourceMappingURL=manager.cjs.map
